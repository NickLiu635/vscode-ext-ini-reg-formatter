[Database]
; This is a standalone comment at the section start
; It should stay at the beginning of the section
; Even after sorting

; This comment should follow the server field
server = localhost

port = 3306

; This comment should follow username
username = admin

password = secret123

[Logging]
; Another standalone comment at section start
; This should remain at the top

; Comment for level field
level = DEBUG

file_path = /var/log/app.log

; Final standalone comment
; This should stay at the end if no field follows